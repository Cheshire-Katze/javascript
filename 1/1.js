let tc = parseFloat(prompt("Укажите температуру по Цельсию")); // объявил переменную tc, введенное значение пользователем будет присвоено tc. ParseFloat потому что температура может быть указана не только целым числом.
let tf = (9 / 5) * tc + 32; // расчет температуры присваиваю переменной tf
parseFloat(alert("Температура по Фаренгейту = " + tf.toFixed(1))); // округляю результат расчетов до 1-го знака и вывожу на экран значение переменной tf